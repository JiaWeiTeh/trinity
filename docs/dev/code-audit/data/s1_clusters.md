# S1 de-duplication — 24 mentions, 17 distinct defects

> ⚠️ **This document may be out of date — verify before trusting it.** It is a
> point-in-time analysis/audit, not a maintained spec; the code moves faster
> than these notes (paths, line numbers, and "what shipped" status drift).
> **Any agent or person reading this: treat it as unverified. Flag that it may
> be stale and re-check each claim, snippet, and line reference against the
> current source before relying on it.**
>
> 🔄 **Living plan — recheck and refine on every visit.** This is an evolving
> strategy doc, not a frozen record. Any agent or person who opens this file
> must, as part of the visit: (1) re-verify the claims and line references above
> against current source; (2) update anything that has drifted; (3) **rethink the
> strategy itself** — if a better ordering, gate, candidate, or experiment
> exists, revise the doc and note what changed and why (date it). Leave it better
> than you found it. **Keep all banner paragraphs at the top of every plan and
> analysis doc.**
>
> 💾 **Persist diagnostics — commit, don't re-run.** The container is ephemeral
> and full/hybr runs cost hours, so any diagnostic worth keeping must be saved as
> a committed artifact under `docs/dev/` (a CSV/table in `docs/dev/data/`, or a
> harness/figure in the relevant `docs/dev/<workstream>/` folder) — never left in
> `/tmp`, the local-only `scratch/`, or an untracked `outputs/`. A future visit must be able to reproduce or compare
> against the numbers **without re-running**; record the exact config + command
> that produced each artifact.
>
> 🔗 **Cross-check the sibling docs — keep the workstream self-consistent.** This file is one of
> several living docs for its workstream (its `PLAN.md`, `FINDINGS.md`, `runs/README.md`, `NOTE_PATCHES.md`,
> and any other notes in the same folder). They drift out of sync *with each other* as fast as they drift
> from the code. Any agent or person editing one MUST, as part of the visit, circle back through the
> siblings and reconcile: if a number, status, claim, or line reference here contradicts a sibling — or a
> sibling has gone stale — fix it (or flag it, dated) so no two docs in the workstream disagree. Never
> update one in isolation.

**Status (2026-07-30):** 🔵 ACTIVE — Phase-5 work list; regenerate the inventory with harness/collect_findings.py before trusting the counts.

> Generated by hand from `data/findings_inventory.csv` (current_severity == S1) on
> 2026-07-30. Regenerate the inventory with `harness/collect_findings.py`; this
> grouping is analysis, not script output. **Phase 5 skeptics are spent per
> *defect*, not per mention** — three lenses each, majority required.

Independent arrival at the same defect from different passes is the strongest
signal this method produces, so the clusters are ranked first.

## Clusters (same defect, multiple independent arrivals)

| # | Defect | Mentions | Passes that found it | Status |
|---|---|---|---|---|
| A | `check_event_termination` returns the first event by **list index**, never reads `event.terminal`; `velocity_sign` (index 0, non-terminal) masks the simulation-ending events at 1-3, and the runner breaks regardless | **4** | S11 reconciler · duplicate-divergence sweep · state sweep · numerical sweep | **verified at source** (resolutions.md#num-02) |
| B | A solver failure does not reach the exit code / termination record | 3 | S11 reconciler · S5b reconciler · silent-failure sweep | **split**: detection claim REFUTED (every runner checks `sol.status`); propagation claim stands |
| C | `EarlyPhaseApproximation` hard override `vd = -1e8` governs the first energy-phase segment; consumed by three phases, reset by one | 2 (+SIGN-08 S2) | magic-number sweep · duplicate-divergence sweep | unverified |
| D | `scipy.integrate.odeint` called with `full_output` ignored and no failure check; a failed solve returns uninitialised memory read as physics | 2 | S8 reconciler · silent-failure sweep | unverified |

## Singletons

| Defect | Id | Status |
|---|---|---|
| `isCollapse` set by substring match; `velocity_runaway_event` matches nothing, so runaway infall is never recorded as collapse — reaches `paper/_lib/plot_markers.py` | S11-R-02 | verified (mechanism) |
| User-set `mu_convert`/`mu_atom`/`mu_ion`/`mu_mol` silently overwritten | S12a-R-01 | **verified at source** |
| `generate_run_name` not injective + no duplicate guard in `emit_jobs` | S12b-R-01 | **verified, reproduced** |
| Shell accelerated by `P_drive = max(Pb, P_HII)` while the energy equation debits PdV only at `Pb` | S4-R-01 | unverified |
| Transition phase evaluates `R1`/`Pb` past the energy->momentum boundary | S6-R-01 | unverified |
| `pdot = 2*Lmech/v_mech`, not `Lmech/v_mech` — a missing factor of 2 | S6-R-02 | unverified |
| `n_IF_Str` (documented sole source of `P_HII`) is identically `shell_n0`, itself back-solved from it | S8-R-02 | unverified |
| Momentum ODE freezes shell mass and `dM/dt` per segment; the other phases recompute | DD-003 | unverified |
| Phase 1a reconciles after an `Eb <= 0` collapse; phase 1b deliberately skips it | DD-004 | unverified |
| Bubble `dMdt` root-find never checks convergence; non-converged `fsolve` returns the seed | SF-002 | unverified |
| `get_residual_pure` swallows every exception into a fixed `(100, 100)` plateau | SF-003 | unverified |
| Momentum RHS clamps `R2`/`mShell` to `1e-10`, fabricating outward acceleration during collapse | SF-005 | unverified |
| Phase 1a's event exit leaves loop-local state one segment stale; the reconciliation snapshot is built from it | ST-001 | unverified |

## Already removed by revision (not S1)

`S9-R-01` CLEARED · `S9-R-02` -> S3 · `S11-R-21` WITHDRAWN — see `data/revisions.csv`.
