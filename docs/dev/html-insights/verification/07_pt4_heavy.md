# Verification ledger — `docs/dev/transition/pt4/pt4_heavy_report.html` (s3 ch2, pt4 heavy clouds)

> ⚠️ **This document may be out of date — verify before trusting it.** Point-in-time
> audit, not a maintained spec; re-check each claim against current source.
>
> 🔄 **Living ledger — recheck and refine on every visit.** Re-run the verdicts when you
> touch the relevant code; tick the fix boxes as they land.
>
> 💾 **Persist diagnostics — commit, don't re-run.** This ledger carries the `file:line` /
> CSV evidence (incl. re-derived trajectory numbers) so a future visit need not re-run the
> monkeypatch matrices. Source artifacts are the committed CSVs under `docs/dev/transition/pt4/`.

Verified: 2026-06-22 against the shared checkout on branch `fix/transition-trigger-problem-pt4`
(HEAD `8dcbf21`), which holds the pt4 report; this ledger is committed in the worktree branch
`worktree-agent-aee174501128f7755`. Target = the report **source** (`make_pt4_heavy_report.py`,
which the committed `pt4_heavy_report.html` is byte-for-byte regenerated from — confirmed
`regenerated == committed: True`). The storyline book (`storyline_s3.html` ch2) only scopes/demotes;
not re-verified line by line. Method: report prose vs `H3_eb_floor_experiment.md`,
`H4_pdvcap_experiment.md`, `h3_eval.csv`, `h4_eval.csv`, `traj/*.csv`, the embedded PNGs (each Read),
current `trinity/` source, and the neighbor chapter `docs/dev/failed-large-clouds/insights.html`.

---

## SUMMARY (6 lines)

- **✅ 34 claims verified** | **⚠️ 4 minor drifts/overstatements** | **❌ 0 factually wrong claims** | **❓ 1 doc-staleness note (out-of-report)**
- **Core thesis VERIFIED.** "Propping up the energy phase does not revive the 5e9 clouds; the honest
  fix is a momentum-driven continuation, and the shipped `ENERGY_COLLAPSED` diagnoses a real physics
  breakdown." H3 (Eb-floor) is REFUTED-as-sole-failure-mode (grind on `fail_repro`, second guard on
  `fail_helix`); H4 (PdV-cap) shows a *survivable transient bought only with non-conserved injected
  energy*. Both are explicitly framed as **monkeypatch-only diagnostics that cannot ship** — caveat
  present in the top banner, §1, §2, §3, and the footer.
- **Every quantitative claim traces to committed CSV/traj.** Spot-re-derived: fail_repro grind
  clamps = 1,206,110 drive / 603,056 state (report "1.2M / 0.6M"); fail_repro tw3e-3 max Eb = 7.913e9
  ("~7.9e9"); PdV/L<1 at t=0.00455 ("~0.0046"); tw1e-2 release PdV/L=0.739 ("0.74"); max_pdv_ratio
  fail_repro 2.6474 / fail_helix 2.3575; controls max rel|ΔEb| ≤ 2.6e-5; 11/13 bit-identical no-op.
- **Equations:** MathJax balanced (200 `$`, 1 display block, 0 brace mismatches); PdV display term
  `4\pi R_2^{2}\,P_b\,v_2` is **bit-identical** to the neighbor chapter (`insights.html:108`) and to
  source `energy_phase_ODEs.py:280`; the 1b balance form matches `get_betadelta.py:434`.
- **Plots:** all 5 embedded figures match captions + prose; the only figure↔prose nuance is the
  `simple_cluster` control's single initial-condition snapshot at PdV/L=2.647 (⚠️-2 below).
- **Drifts are all in the source DEV DOCS, not the rendered report** — the report cites no production
  line numbers, so the `:1007`→`:1072` drift (⚠️-1) does not appear in the verification target.

---

## Core-thesis statement

> The heaviest clouds (`mCloud=5e9`) drain `E_b` via real PdV expansion work
> (`PdV/L_mech = 4πR₂²P_b v₂ / L_mech` > 1 for an extended early epoch); the energy phase then
> collapses and now stops cleanly as `ENERGY_COLLAPSED`. Two rescue diagnostics — floor `E_b` (H3),
> cap the PdV drain (H4) — **fail to revive these clouds without injecting non-conserved energy**.
> The honest fate is a **momentum-driven continuation**, not an energy-phase prop; `ENERGY_COLLAPSED`
> diagnoses a real breakdown rather than hiding a tractable continuation.

**Verdict: the core thesis is fully supported by the committed evidence and current source.**

---

## Per-claim table

| Claim | Report § | Evidence (file:line / CSV) | Verdict | Fix |
|---|---|---|---|---|
| Report HTML is regenerated byte-identical from `make_pt4_heavy_report.py` + 5 committed PNGs | (whole) | `regenerated == committed: True`; 5 placeholders `__FIG_*__` map 1:1 to 5 `<figure>`/`<img>` | ✅ | — |
| Both experiments are monkeypatch-only; production untouched | banner/§0/§3/footer | top `.banner`, §1, §2, §3 callout, footer all state it; H3.md/H4.md §0 caveats; no `trinity/` edits in branch | ✅ | — |
| PdV/Lmech control ratio = `4πR₂²P_b v₂/L_mech` | callout | source `energy_phase_ODEs.py:280` `(4*np.pi*R2**2*press_bubble)*v2`; neighbor `insights.html:108` identical | ✅ | — |
| H3: floor `E_b=max(E_b,1e-3)` + reflecting `dE_b/dt≥0`; injects energy; diagnostic only | §1 | `H3_eb_floor_experiment.md` §0,§2; floor 1e-3 [au] | ✅ | — |
| H3 REFUTED: `E_b`-collapse is NOT the sole failure mode | §1 callout | `H3…md` §6 verdict; trajectory evidence §4 | ✅ | — |
| H3: bit-identical no-op on **11 of 13** healthy/stall configs; other 2 differ ~1e-8 | §1 | `h3_noop_diffs.txt`: 11 rows `max|dR2|=0, max rel|dEb|=0`; small_dense_highsfe 5.9e-8, small_1e5 6.8e-9 | ✅ | — |
| H3 `fail_repro`: V0 ENERGY_COLLAPSED, one implicit step `E_b: 4.75e8→−9.14e8`, stop t=0.00341, R2=9.73 pc | §1 table | `h3_eval.csv` (V0 final_Eb=−9.14e8, t=0.00341, R2=9.73); `h4_eval.csv` fail_repro,V0 same | ✅ | — |
| H3 `fail_repro` EBFLOOR: energy phase bit-identical (52/52), implicit `solve_ivp` grinds ~1.2M drive + 0.6M state clamps, R2 frozen 8.61 pc, killed by 300 s timeout | §1 table/§prose | `h3_eval.csv` act_drive=1206110, act_state=603056, final_R2=8.606902, min_Eb_seen=−2.59e7; `h3_noop_diffs.txt` fail_repro 52 rows identical | ✅ | — |
| H3 `fail_helix`: V0 & EBFLOOR byte-identical, `E_b=0.005085287954767937` to the digit, 2nd independent guard fires (1a) | §1 table | `h3_eval.csv` both rows final_Eb=0.005085287954767937, code 51, phase 1a; reason "bubble solve degenerate as Eb -> 0" | ✅ | — |
| H3 second guard = phase-1a try/except around bubble-structure solve as `E_b→0` | §1 | `run_energy_phase.py:169-183` (try/except `get_bubbleproperties_pure`, reason "bubble solve degenerate as Eb -> 0", code `ENERGY_COLLAPSED`) | ✅ | — |
| H3: cooling-balance trigger NEVER fires under EBFLOOR; `rmin` 0.50–0.99 (>100× from 0.05) | §1 | `h3_eval.csv` all `trigger_fired=False`; ratio_min 0.496–0.997 | ✅ | — |
| H3: collapse regime is narrow & mass-gated — only 5e9 collapses early; 5e8/1e9 stay `E_b>0` | §1 | `h3_eval.csv` mass_5e8 Eb=2.93e9>0, mass_1e9 Eb≈2.7–3.0e9>0 (slow/healthy) | ✅ | — |
| H4: cap drain to `min(PdV, κL_mech)`, κ=0.9, leaves shell drive `vd` untouched; injects energy; diagnostic | §2 | `H4…md` §0,§2; source: PdV in `Ed` (`energy_phase_ODEs.py:280`) separate from `P_drive` (`:258,265`) | ✅ | — |
| H4: PdV/L stays >1 for ~1.5–4.5e-3 Myr from phase start | §2/§3 callout | `H4…md` §6; traj `fail_repro_V0` PdV>1 over t=0.00138–0.00291, `fail_helix_V0` 0.00098–0.00255 | ✅ | — |
| H4 `t_win=1e-3` collapses (byte-identical to V0; cap never bites on fail_repro), `3e-3` splits the two clouds, `1e-2` self-sustains both | §2 table | `h4_eval.csv`: fail_repro tw1e-3 code51 t=0.0034 (cap_act False); tw3e-3 code51 t=0.00515; tw1e-2 survived/selfsust True; fail_helix tw3e-3 selfsust True | ✅ | — |
| H4 fail_repro tw3e-3: `E_b` inflated to 7.9e9, PdV/L crosses<1 at t~0.0046, ENERGY_COLLAPSED t=0.00515 | §2 table | traj re-derived: max Eb=7.913e9, PdV/L<1 first at t=0.00455, final_t=0.00515 (code 51) | ✅ | — |
| H4 fail_repro tw1e-2: ~10× window builds `E_b≈2.2e10`, PdV/L→0.74 at release, self-sustains | §2 table | `h4_eval.csv` final_Eb=2.18e10, PdV/L_after=0.738; traj release PdV/L=0.739, Eb=1.57e10 rising | ✅ | — |
| H4 `~10× injected energy` framing | §2/§3 | tw1e-2 / tw1e-3 = 10×; reservoir 2.2e10 vs natural peak ~6.4e9 (≈order of magnitude) | ✅ | — |
| H4 `t_win=1e-1` rows read "no" only due to run truncation (cap-never-released control) | §2 table/Fig 4 cap | `h4_eval.csv` tw0.1 final_t 0.013–0.03 < 0.1, survived=False, high growing final_Eb | ✅ | — |
| H4 controls (`simple_cluster`,`pl2_steep`,`small_1e6`): clean STOPPING_TIME, no-op max rel|ΔEb| ≤ 2.6e-5 | §2 "control" | `h4_eval.csv` all code 1; `H4…md` §5: small_1e6 2.6e-5, simple_cluster 3.0e-8, pl2_steep 1.9e-8 | ✅ | — |
| H4 max_pdv_ratio fail_repro 2.65 / fail_helix 2.36 | §2/Figs | `h4_eval.csv` 2.6474 / 2.3575 | ✅ | — |
| §3 thesis: fix is momentum, not energy props; `ENERGY_COLLAPSED` diagnoses real breakdown | §3 | `H3…md` §6, `H4…md` §6 verdicts; neighbor `insights.html` established PdV→collapse→ENERGY_COLLAPSED | ✅ | — |
| §3 "unifying picture": stall (ratio <1, trigger sleeps) vs 5e9 crash (ratio >1) are opposite extremes of one PdV/L axis | §3 | consistent with neighbor `insights.html` and pt4 s1 transition report; ratio framing internally coherent | ✅ | — |
| Reproduce commands point at committed harness scripts | §4 | `h3_run_matrix.sh`, `h3_analyze.py`, `h4_run_matrix.sh`, `h4_analyze.py`, `make_pt4_figures.py`, `h4_figures.py` all present | ✅ | — |
| (source-doc) 1b `Eb≤0` state guard at `run_energy_implicit_phase.py:1007` | H3.md:67/91/165, H4.md:70 | **Actual `:1072`** — `:1007` is now the `except Exception` around `solve_ivp`. ~65-line drift. **NOT in the rendered report** (report cites no prod line numbers). | ⚠️ drift (docs only) | Update H3.md/H4.md `:1007`→`:1072` |
| (source-doc) 1b implicit RHS call site `:929` / signature `:532` | H3.md:84, H4.md:101,106 | `get_ODE_implicit_pure` def at `:550`; implicit `solve_ivp` `fun=...get_ODE_implicit_pure` at `:994`; `[rd,vd,Ed_from_beta,Td]` doc'd at the func, not `:532` (that's `ImplicitPhaseResults`) | ⚠️ drift (docs only) | Update H3/H4 line refs to `:550`/`:994` |
| Fig 5 prose: `simple_cluster` "PdV/L stays below 1, cap never bites" | Fig 5 caption + §"control" | traj `simple_cluster_V0`: exactly **1 of 104** snapshots >1 — the t=3e-7 IC handoff spike (PdV/L=2.647, same first-step artifact all configs show); thereafter ~0.4, never re-crosses. Fig 5 shows that descending early segment as "above 1". `max_pdv_ratio_in_window=0.909`. | ⚠️ minor overstatement | Optionally caption "stays below 1 after the IC handoff spike" |
| neighbor uses `\text{PdV}` ; pt4 uses `\rm PdV` / bare `PdV` in mixed text | EQUATIONS | both render upright "PdV"; symbols `E_b,P_b,v_2,R_2,R_1,L_{\rm mech}` identical across chapters | ⚠️ cosmetic | none needed |
| (out-of-report, README) `fail_helix` 2nd guard described as "xi out of bounds" | `pt4/README.md:60` | report + H3.md + source reason string say "cooling table out of bounds" / "bubble solve degenerate as Eb -> 0" (`run_energy_phase.py:174`). README phrasing is looser; report is correct. | ❓ inconsistency (README only) | Optionally align README to "cooling table out of bounds" |

---

## EQUATIONS sub-section

- **Syntax / balance:** rendered HTML has 200 `$` (even → balanced), one `$$…$$` display block, 98
  inline spans, **0 brace mismatches**. MathJax v3 CDN config present
  (`inlineMath [['$','$'],['\(','\)']]`, displayMath `[['$$','$$'],['\[','\]']]`). All escapes valid.
- **Display equation** (HTML §callout): `\frac{\rm PdV}{L_{\rm mech}}=\frac{4\pi R_2^{2}\,P_b\,v_2}{L_{\rm mech}}`.
  Numerator `4\pi R_2^{2}\,P_b\,v_2` is **bit-identical** to the neighbor chapter
  `failed-large-clouds/insights.html:108` (`4\pi R_2^{2}\,P_b\,v_2`) and to production source
  `trinity/phase1_energy/energy_phase_ODEs.py:280`: `(4 * np.pi * R2**2 * press_bubble) * v2`. ✅
- **1b balance form** (referenced in §3 and the source docs):
  `Edot_from_balance = L_gain − L_loss − 4πR²·v2·Pb` matches `get_betadelta.py:434` exactly. ✅
- **Notation consistency:** `E_b, P_b, v_2, R_2, R_1, L_{\rm mech}, \kappa, t_{\rm window}` used
  consistently within the chapter and with the neighbor chapter. Only `\rm PdV` vs `\text{PdV}`
  differs cosmetically (both upright). ✅ (⚠️ cosmetic only)
- **Physical correctness:** `E_b=\max(E_b,10^{-3})`, `\min({\rm PdV},\kappa L_{\rm mech})`,
  injected excess `{\rm PdV}-\kappa L_{\rm mech}` all match the harness design in H3/H4 §2. ✅

## PLOTS sub-section (each PNG Read)

1. **Fig 1 `h3_ebfloor_noop_and_grind`** — Left: `fail_repro` `$E_b(t)$`, V0 (blue) peaks ~6.45e9 at
   t≈0.0015, crosses 0 at t≈0.0031 with "Eb=0 → ENERGY_COLLAPSED" line; EBFLOOR (orange dashed)
   overlays then halts ~0.0029 (grind freeze). Right: `simple_cluster` control, log-log, V0 (thick)
   + EBFLOOR overlay exactly, "max rel|dEb|=0 → bit-identical no-op". Matches caption + §1 prose. ✅
2. **Fig 2 `h4_Eb_sweep_fail_repro`** — symlog `$E_b(t)$`; V0 (black) collapses through 0 at
   t≈0.0034; dotted releases at 1e-3/3e-3/1e-2/1e-1; tw1e-3 hidden behind V0 (byte-identical),
   tw3e-3 inflates ~8e9 then dips <0 (re-collapse), tw1e-2/1e-1 grow (self-sustain). ✅
3. **Fig 3 `h4_pdvratio_sweep_fail_repro`** — PdV/Lmech(t); V0 spikes to ~2.65 (=max_pdv_ratio
   2.6474), hump ~1.55; break-even line at 1; capped runs descend through 1 (tw3e-3 ~0.005,
   tw1e-2/1e-1 to ~0.7). Negative collapse spike clipped at y=−0.5 as caption states. ✅
4. **Fig 4 `h4_summary`** — survived (solid)/self-sustained (dashed) vs t_window; fail_repro
   solid no→no→yes→no, self-sust dashed yes only at 1e-2; fail_helix self-sust flips yes by 3e-3;
   mass_1e9 yes throughout (self-sust even at 1e-3); all drop to "no" at 1e-1 (truncation control).
   Matches prose "fail_helix flips by 3e-3; fail_repro needs 1e-2". ✅
5. **Fig 5 `h4_control_vs_collapse`** — y-axis `4πR₂²P_b v₂/L_mech` (= display eq); `simple_cluster`
   (green) starts ~2.6 at t≈3e-7 (single IC point), falls below 1 by t≈1.3e-5, stays ~0.4, never
   re-crosses; fail_repro/fail_helix (orange/blue) appear at t~1e-3 and cross 1 (to 2.65/1.5). The
   claim "5e9 clouds cross 1 while control stays <1" holds for the operative integration; the green
   curve's early >1 IC point is the only nuance (⚠️-2). ✅ (with note)
- **Coverage:** all 5 referenced figures embedded; all 5 embedded figures referenced; no
  wrong/duplicated figure; the unused PNGs (`h4_Eb_sweep_fail_helix/mass_1e9`,
  `h4_pdvratio_sweep_fail_helix/mass_1e9`, `h1_*`, `h2_*`, `r1_*`) belong to other chapters and are
  correctly NOT embedded here. ✅

## FLOW / NARRATIVE sub-section

Reads as a coherent, properly-sequenced continuation of the neighbor chapter. The lead explicitly
hands off from "Failed large clouds" (Eb drained by PdV → ENERGY_COLLAPSED) and poses the natural
follow-up ("can we keep them alive?"). The two experiments are ordered symptom→cause (H3 backstops
the symptom by flooring `E_b`; H4 throttles the cause by capping PdV) and converge in §3 on the same
conclusion, then close the loop with the rest of the storyline (stall vs crash as opposite extremes
of one PdV/Lmech axis) and point at the deferred momentum-handoff family `T`. Voice/tense consistent;
no internal contradictions; no contradiction with the prior chapter or the source docs.
**Diagnostic-not-fix caveat is unmistakably present and repeated** — top yellow banner
("monkeypatch-only — production code is untouched"), §1 ("injects energy and violates
conservation… a diagnostic… not a fix candidate"), §2 ("UNDER-DRAINS… INJECTS ENERGY… a diagnostic,
not a fix"), §3 ("the 'rescue' is exactly the injected, non-conserved energy"), and the footer.
**Flow assessment: strong; no awkward transitions.** The only narrative softness is the Fig-5
control wording (⚠️-2), which slightly understates the shared IC spike.

---

## ❌ / ⚠️ / ❓ list (location + recommended fix)

- **❌ none.**
- **⚠️-1 (docs only, not in report):** `H3_eb_floor_experiment.md:67,91,165` and
  `H4_pdvcap_experiment.md:70` cite the 1b `Eb≤0` guard at `run_energy_implicit_phase.py:1007`;
  it is now at **`:1072`** (`:1007` is the `except Exception` clause around `solve_ivp`). **Does not
  appear in the rendered report** (no prod line numbers there), so no `make_pt4_heavy_report.py`
  patch needed — fix the two source docs.
- **⚠️-2 (report wording):** Fig 5 caption + §"control parameter" say `simple_cluster`
  "PdV/L stays below 1." The control's first (IC-handoff) snapshot is PdV/L=2.647 (1 of 104 rows),
  the same first-step artifact all configs show; thereafter it stays ~0.4. *Recommended fix in*
  `make_pt4_heavy_report.py`: soften to "stays below 1 **after the initial-condition handoff
  spike**" (Fig 5 figcaption + the §"control parameter cleanly separates the regimes" paragraph).
  Substantive claim (cap is a no-op, ≥4 sig figs) is unaffected.
- **⚠️-3 (docs only):** `H3.md:84`, `H4.md:101,106` 1b call-site/signature line refs (`:929`,
  `:532`) drifted — implicit RHS def now `:550`, its `solve_ivp` call `:994`. Not in the report.
- **⚠️-4 (cosmetic):** neighbor uses `\text{PdV}`, pt4 uses `\rm PdV`/bare `PdV`. Both render
  upright; no change required.
- **❓-1 (out-of-report, README only):** `pt4/README.md:60` summarizes the fail_helix 2nd guard as
  `"xi out of bounds"`; the report, `H3.md`, and the production reason string
  (`run_energy_phase.py:174`) all say "cooling table out of bounds" / "bubble solve degenerate as
  Eb -> 0". Report is correct; optionally align README.
