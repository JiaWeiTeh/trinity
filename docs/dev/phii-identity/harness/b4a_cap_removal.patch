diff --git a/trinity/shell_structure/shell_structure.py b/trinity/shell_structure/shell_structure.py
index 18af1b3..652557a 100644
--- a/trinity/shell_structure/shell_structure.py
+++ b/trinity/shell_structure/shell_structure.py
@@ -249,8 +249,11 @@ def shell_structure_pure(params) -> ShellProperties:
             (4.0 * np.pi * params['chi_e_shell'].value * params['caseB_alpha'].value * _vol_ion)
         )
         n_IF_Str_raw = n_IF_Str  # Preserve pre-cap value for diagnostics
-        # Cap: thin ionised skin → P_HII cannot exceed P_b
-        n_IF_Str = min(n_IF_Str, shell_n0)
+        # BATCH 4a (C2a) — cap REMOVED for this arm only. Stock code caps the
+        # Strömgren density at shell_n0, which makes P_HII an exact relabelling
+        # of the confining pressure (docs/dev/phii-identity/). This arm measures
+        # what happens without it. Do not merge without the PLAN.md §6 verdict.
+        # n_IF_Str = min(n_IF_Str, shell_n0)
     else:
         n_IF_Str = 0.0
         n_IF_Str_raw = 0.0
