# TRINITY Core Dependencies
# Install with: pip install -r requirements.txt

# Core scientific computing
# numpy and scipy are exact-pinned to the dev environment because scipy>=1.15 +
# numpy>=2 changed the LSODA integrator's input validation in ways that trip the
# bubble-structure code path. Astropy/matplotlib/pandas remain range-pinned —
# upper caps exclude the next, untested major.
numpy==1.22.4
scipy==1.10.1
astropy>=5.0,<8
matplotlib>=3.5.0,<4
pandas>=1.3.0,<3

# Optional: Development tools (uncomment if needed)
# pytest>=7.0
# pytest-cov>=4.0
# black>=23.0
# flake8>=6.0
