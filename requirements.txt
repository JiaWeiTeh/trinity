# TRINITY Core Dependencies
# Install with: pip install -r requirements.txt

# Core scientific computing
# Upper caps exclude the next, untested major (which tends to ship breaking
# changes). If you already have a newer version, run.py warns at startup and
# points you here to install the supported set.
numpy>=1.20.0,<3
scipy>=1.7.0,<2
astropy>=5.0,<8
matplotlib>=3.5.0,<4
pandas>=1.3.0,<3

# Optional: Development tools (uncomment if needed)
# pytest>=7.0
# pytest-cov>=4.0
# black>=23.0
# flake8>=6.0
